apiVersion: v1
kind: Service

metadata:
  name: nginx-service1

spec:
  selector:
    app: nginx-service-name1

  ports:
    - port: 7000
      targetPort: 80
      nodePort: 30487
      
  type: NodePort



